server {
    listen 0.0.0.0:5000;
    server_name localhost;

    # max upload size
    client_max_body_size 75M;
    
    underscores_in_headers on;

    location / {
        proxy_pass_request_headers on;
        proxy_set_header eppn 'nem2p@virginia.edu';
        include uwsgi_params;
        uwsgi_pass unix:///ithriv_service/app.sock;
    }
}

